Exercise 4 - Column Layouts and Grid Classes

Objective

Create responsive layouts using Bootstrap Grid classes.

Scenario

Design different page layouts using Bootstrap's grid system.

Tasks

- Create a two-column layout with a sidebar (`col-md-3`) and content area (`col-md-9`).
- Create a four-column layout using `col-sm-3`.
- Use Bootstrap's grid system for responsive design.

Files

- index.html
