Exercise 7 - DOM Manipulation

Objective

Render community events dynamically using JavaScript DOM manipulation.

Scenario

Display all events dynamically on the webpage.

Tasks

- Access DOM elements using `querySelector()`.
- Create event cards using `createElement()`.
- Append event cards dynamically.
- Update the UI when a user registers or cancels registration.

Files

- index.html
- main.js
