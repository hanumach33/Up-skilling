Exercise 3 - Conditionals, Loops, and Error Handling

Objective

Use conditionals, loops, and error handling in JavaScript.

Scenario

Only display upcoming events that have available seats and handle invalid registrations.

Tasks

- Use `if-else` to hide past or full events.
- Loop through the event list using `forEach()`.
- Wrap the registration logic inside a `try-catch` block.

Files

- index.html
- main.js
